# MAX30101 PPG 传感器本项目配置说明

> 基于 `Core/Src/MAX30101.c`、`MAX30101_2.c`、`Core/Inc/main.h`、`Core/Inc/sample_rate_config.h` 和 `Core/Src/main.c` 实际配置整理。
> 详细的寄存器位域说明请参考 `docs/hardware/MAX30101寄存器配置说明.md` (数据手册总结)。

---

## 1. 硬件接口

本项目使用 **两片 MAX30101**, 分别通过独立的软件 I2C 总线连接:

| 通道 | 源文件 | I2C 总线 | 7-bit 地址 | 通信方式 |
|------|--------|---------|-----------|---------|
| PPG_CH1 | MAX30101.c/h | IIC1 (软件模拟) | 0x57 | 阻塞式 GPIO 位操作 |
| PPG_CH2 | MAX30101_2.c/h | IIC2 (软件模拟) | 0x57 | 阻塞式 GPIO 位操作 |

通道选择由 `main.h` 中 `PPG_DEFAULT_CHANNEL` 决定, 默认使用通道 2 (IIC2 总线)。

---

## 2. 工作模式概览

本项目支持两种 PPG 工作模式, 由 `main.h` 中 `CURRENT_WORK_MODE` 编译时选择:

| 模式 | 宏值 | LED 组合 | FIFO 每样本字节数 |
|------|------|---------|------------------|
| 心率模式 (默认) | MODE_HEART_RATE (0) | Green + Red + IR (Multi-LED) | 9 字节 |
| 血氧模式 | MODE_SPO2 (1) | Red + IR (SpO2) | 6 字节 |

当前默认编译配置为 `CURRENT_WORK_MODE = MODE_HEART_RATE`、`ENABLE_RAW_DATA_PACKET = 1`、`PPG_DEFAULT_CHANNEL = 2`、`PPG_SAMPLE_RATE = 100`。

---

## 3. 心率模式配置 (Multi-LED, 三光路)

### 3.1 初始化序列 (`MAX30101_Init` + `PPG_Config_Green_Hardcoded`)

**第一步: MAX30101_Init() 通用初始化**

```
0x00 = 0x00    // 清除中断状态1
0x01 = 0x00    // 清除中断状态2
0x02 = 0x00    // 关闭所有中断
0x03 = 0x00    // 关闭温度中断
0x04 = 0x00    // FIFO_WR_PTR 清零
0x05 = 0x00    // OVF_COUNTER 清零
0x06 = 0x00    // FIFO_RD_PTR 清零
0x08 = 0x10    // FIFO: 无平均 + ROLLOVER_EN + A_FULL=0
0x09 = 0xC7    // MODE: SHDN + RESET + Multi-LED (通用初始化临时状态, 后续覆写)
0x0A = 0x7F    // SPO2: ADC=16384nA + SR=3200sps + PW=411us/18bit
0x0C = 0xB1    // Red LED 电流 ~35mA
0x0D = 0xB1    // IR LED 电流 ~35mA
0x0E = 0xB1    // Green LED 电流 ~35mA
0x11 = 0x01    // SLOT1=Red, SLOT2=Disabled
0x12 = 0x00    // SLOT3/4=Disabled
```

**第二步: PPG_Config_Green_Hardcoded() 覆写为三光路配置**

```
0x09 = 0x07    // MODE: Multi-LED (不复位, 不休眠)
0x11 = 0x13    // SLOT1=Green(011), SLOT2=Red(001)
0x12 = 0x02    // SLOT3=IR(010), SLOT4=Disabled
0x0E = 0xE0    // Green LED 电流: 增大电流对照组
0x0C = 0xC0    // Red LED 电流: 增大电流对照组
0x0D = 0xC0    // IR LED 电流: 增大电流对照组
0x0A = 由采样率决定 (见下表)
0x08 = 由采样率决定 (见下表)
0x04~06 = 0x00 // 再次清空 FIFO 指针
```

### 3.2 采样率相关寄存器 (sample_rate_config.h)

本项目固定 `PPG_SAMPLE_RATE = 100`, 对应参数:

| 寄存器 | 地址 | 值 | 说明 |
|--------|------|-----|------|
| SPO2_CONFIG | 0x0A | **0x6F** | ADC=16384nA, SR=400sps, PW=411us (18-bit) |
| FIFO_CONFIG | 0x08 | **0x3F** | SMP_AVE=2x, ROLLOVER_EN, A_FULL=15 |

**采样率关系**: 内部 ADC 以 400sps 采样, 2x 硬件平均后传感器有效输出 200sps (`400sps / 2x`), MCU 仍按 100Hz 轮询/发包。当前暂定采集方案: 以 `0x6F/0x3F` 保持 411us 脉宽 / 18-bit 分辨率和 16384nA 量程, 同时使用增大电流 LED PA (`Green=0xE0`, `Red/IR=0xC0`)。

**三光路时序约束**: 3 LED x 411us = 1233us < 2500us (1/400sps), 余量 50.7%, 时序安全。

### 3.3 各采样率参数对比

| 采样率 | TIM16 ARR | SPO2_CONFIG | FIFO_CONFIG | ADC内部 | 平均 | 脉宽 | 三光路状态 |
|--------|-----------|-------------|-------------|---------|------|------|------------|
| 50Hz | 1999 | 0x73 | 0x9F | 800sps | 16x | 411us (18-bit) | 可编译 |
| **100Hz** | **999** | **0x6F** | **0x3F** | **400sps** | **2x** | **411us (18-bit)** | **当前已部署方案** |
| 125Hz | 799 | 0x77 | 0x5F | 1000sps | 4x | 411us (18-bit) | 暂不可用 (`#error`) |

> `sample_rate_config.h` 当前允许 50Hz 和 100Hz 三光路 (G+R+IR) 编译; 125Hz 分支因 3 x 411us > 1/1000sps 暂未适配, 会触发编译期错误。当前项目默认使用 100Hz。

### 3.4 当前暂定方案的实测质量摘要

近期对比 `0x6F/0x3F`、`0x76/0x5F + 增大电流`、`0x6F/0x3F + 增大电流` 后，当前暂定保持 `0x6F/0x3F + Green=0xE0, Red/IR=0xC0`。该方案链路层未见丢包，FIFO 空读约 3.65%，三重零差率 3.8%，仍维持 100Hz Raw 输出和 Q4 小数均值。

| 指标 | Green | Red | IR |
|------|------:|----:|---:|
| DC 均值 | 7855.2 | 111076.3 | 165724.9 |
| 心率带 AC/DC | 0.4090% | 0.0188% | 0.0436% |
| 心率峰 SNR | 31.93dB | 30.99dB | 31.61dB |
| 单通道零差率 | 12.7% | 11.0% | 8.2% |
| 唯一值数量 | 758 | 802 | 1469 |
| 低频/心率带 | 3.91 | 3.71 | 3.12 |

相对基础 `0x6F/0x3F`，增大电流版本提升了 DC、心率带 AC/DC、唯一值数量并降低零差率，Red 的心率峰 SNR 也提高；主要代价是低频漂移相对心率带增大。后续数据分析应同时报告 DC、AC/DC、心率峰 SNR、低频/心率带、零差率和 FIFO 空读率，具体计算方式见 `docs/hardware/MAX30101采集策略评估与优化建议.md` 的“质量指标计算方式”。

---

## 4. 血氧模式配置 (SpO2, Red+IR)

当 `CURRENT_WORK_MODE = MODE_SPO2` 时, 由 `PPG_Config_SpO2_Hardcoded()` 配置:

```
0x09 = 0x03    // MODE: SpO2 (Red + IR)
0x0C = 0x91    // Red LED 电流 ~29mA
0x0D = 0x91    // IR LED 电流 ~29mA
0x0A = 0x77    // ADC=16384nA + SR=1000sps + PW=411us (18-bit)
0x08 = 0x5F    // SMP_AVE=4x + ROLLOVER_EN + A_FULL=15
0x04~06 = 0x00 // 清空 FIFO 指针
```

双通道时序: `2 x 411us = 822us < 1000us (1/1000sps)`, 余量 17.8%。有效输出 `1000sps / 4x = 250sps`, MCU 100Hz 轮询下每周期约 2.5 个 FIFO 样本, Q4 均值产生小数。

### 4.1 与心率模式差异

| 项目 | 心率模式 | 血氧模式 |
|------|---------|---------|
| MODE 寄存器 | 0x07 (Multi-LED) | 0x03 (SpO2) |
| 活跃 LED | Green + Red + IR | Red + IR |
| LED 电流 | Green=0xE0, Red/IR=0xC0 (增大电流对照组) | Red/IR=0x91 (~29.0mA) |
| ADC 量程 | 16384nA | 16384nA |
| 内部采样率 | 400sps | 1000sps |
| 硬件平均 | 2x | 4x |
| ADC 分辨率 | 18-bit | 18-bit |
| 有效输出率 | 200sps | 250sps |
| FIFO 每样本 | 9 字节 | 6 字节 |
| PPG 精度 | Q4 固定小数 | Q4 固定小数 |

---

## 5. FIFO 数据读取

### 5.1 读取流程 (主循环, 每次 ADC 完成后)

```
1. 读取 FIFO_WR_PTR (0x04) 和 FIFO_RD_PTR (0x06)
2. 计算可用样本数: sample_count = (wr_ptr - rd_ptr) & 0x1F
3. 循环读取 sample_count 个样本:
   - 心率模式: 每样本 9 字节 (Green 3B + Red 3B + IR 3B)
   - 血氧模式: 每样本 6 字节 (Red 3B + IR 3B)
4. 多样本取均值并以 Q4 固定小数打包, 缓存上次有效值防止空读跳变
```

### 5.2 数据解包 (18-bit, 心率模式)

```c
// 24-bit 左对齐原始数据, 直接掩码 18-bit 有效值
uint32_t raw = (buf[0] << 16) | (buf[1] << 8) | buf[2];
uint32_t value = raw & 0x03FFFF;

// 固件对同一 Raw 包内读到的 FIFO 样本求平均后左移 4 bit 打包:
// ppg_q4 = round(mean(value) * 16)
// 上位机解析时再除以 16.0, 因此 CSV 中 PPG 列允许出现 .0625 步进的小数。
```

### 5.3 数据解包 (18-bit, 血氧模式)

```c
// 24-bit 左对齐原始数据, 直接掩码 18-bit 有效值
uint32_t value = (buf[0] << 16 | buf[1] << 8 | buf[2]) & 0x03FFFF;

// 固件对同一 Raw 包内读到的 FIFO 样本求平均后左移 4 bit 打包:
// ppg_q4 = round(mean(value) * 16)
// 上位机解析时再除以 16.0, 因此 CSV 中 PPG 列允许出现 .0625 步进的小数。
// Green 通道恒为 0。
```

---

## 6. 数据协议映射

### 6.1 心率模式 (35 字节原始数据包)

PPG 数据位于 `allData[PPG_START_INDEX]` 起始的 9 字节:

| 偏移 | 字段 | 说明 |
|------|------|------|
| 22~24 | PPG Green | Q4 均值, 高位在前, 上位机除以 16 还原为原始码值 |
| 25~27 | PPG Red | Q4 均值, 高位在前, 上位机除以 16 还原为原始码值 |
| 28~30 | PPG IR | Q4 均值, 高位在前, 上位机除以 16 还原为原始码值 |
| 31~32 | Seq | 固件侧Raw采样序号, uint16 BE |
| 33 | XOR | bytes[2..32]异或 |
| 34 | 帧尾 | 0xCC |

### 6.2 血氧模式

PPG 数据布局与心率模式统一 (Green=0), 上位机解析逻辑无需修改:

| 偏移 | 字段 | 说明 |
|------|------|------|
| 22~24 | PPG Green | 恒为 0x000000 (无绿光通道) |
| 25~27 | PPG Red | Q4 均值, 高位在前, 上位机除以 16 还原为原始码值 |
| 28~30 | PPG IR | Q4 均值, 高位在前, 上位机除以 16 还原为原始码值 |
| 31~32 | Seq | 固件侧Raw采样序号, uint16 BE |
| 33 | XOR | bytes[2..32]异或 |
| 34 | 帧尾 | 0xCC |

---

## 7. PPG 通道选择机制

`ppg_channel.c/h` 封装了通道选择, 所有操作通过 `PPG_` 前缀统一调用:

```c
PPG_SetChannel(PPG_CH2);      // 切换到通道2
PPG_Init();                    // 初始化当前通道的 MAX30101
PPG_ReadOneByte(0xFF);         // 读当前通道寄存器
PPG_WriteOneByte(0x09, 0x07);  // 写当前通道寄存器
PPG_ReadFIFO_Burst(buf, 9);   // 批量读当前通道 FIFO
```

---

## 8. 寄存器速查表 (心率模式 100Hz)

| 地址 | 名称 | Init值 | Config覆写值 | 说明 |
|------|------|--------|-------------|------|
| 0x00~0x03 | 中断 | 全0 | - | 不使用中断 |
| 0x04~0x06 | FIFO指针 | 0x00 | 0x00 | 每次配置后清零 |
| 0x07 | FIFO_DATA | 只读 | - | 批量读取窗口 |
| 0x08 | FIFO_CONFIG | 0x10 | **0x3F** | 2x平均 + 回滚 |
| 0x09 | MODE_CONFIG | 0xC7 | **0x07** | Multi-LED |
| 0x0A | SPO2_CONFIG | 0x7F | **0x6F** | 16384nA + 400sps + 18bit |
| 0x0C | LED1_PA (Red) | 0xB1 | **0xC0** | 增大电流对照组 |
| 0x0D | LED2_PA (IR) | 0xB1 | **0xC0** | 增大电流对照组 |
| 0x0E | LED3_PA (Green) | 0xB1 | **0xE0** | 增大电流对照组 |
| 0x11 | MULTI_LED_CTRL1 | 0x01 | **0x13** | SLOT1=Green, SLOT2=Red |
| 0x12 | MULTI_LED_CTRL2 | 0x00 | **0x02** | SLOT3=IR, SLOT4=Off |
| 0xFF | PART_ID | 只读 | - | 期望值 0x15 |
